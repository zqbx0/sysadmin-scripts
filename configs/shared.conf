#!/bin/bash
# 共享配置

# 通用端口范围（避免冲突）
PORT_RANGES=(
    ["sing-box"]="10000-10010"
    ["sba"]="10100-10105" 
    ["argox"]="10200-10205"
    ["hysteria2"]="10300"
    ["4in1"]="10400-10403"
)

# 共享UUID（可选）
SHARED_UUID=$(cat /proc/sys/kernel/random/uuid 2>/dev/null || echo "")

# CDN设置
DEFAULT_CDN="cdn.anycast.eu.org"

# Argo默认设置
ARGO_DOMAIN_PREFIX="proxy"
